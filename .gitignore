# SwiftPM
.build/
.swiftpm/
Package.resolved

# Node (JS bundle build step)
js/node_modules/

# code-index MCP (локальный индекс)
.code-index/

# macOS
.DS_Store

# Xcode
*.xcodeproj
*.xcuserstate
xcuserdata/
DerivedData/
test-samples/
scratchpad/
test-vsix/

# Артефакты ИИ-разработки: остаются на диске для разработки,
# но не попадают в публичный репозиторий.
CLAUDE.md
docs/superpowers/

# Локальные архивы скриншотов (напр. docs/screenshots/Архив.zip)
docs/screenshots/*.zip
